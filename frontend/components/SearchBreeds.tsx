import { SearchBreedsProps } from '@/types'
import React from 'react'

const SearchBreeds = ({MenuBreeds,setMenuBreeds}:SearchBreedsProps) => {
  return (
    <div>SearchBreeds</div>
  )
}

export default SearchBreeds